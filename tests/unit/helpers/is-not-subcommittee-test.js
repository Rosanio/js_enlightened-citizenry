import { isNotSubcommittee } from 'enlightened-citizenry/helpers/is-not-subcommittee';
import { module, test } from 'qunit';

module('Unit | Helper | is not subcommittee');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isNotSubcommittee([42]);
  assert.ok(result);
});
