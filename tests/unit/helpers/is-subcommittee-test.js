import { isSubcommittee } from 'enlightened-citizenry/helpers/is-subcommittee';
import { module, test } from 'qunit';

module('Unit | Helper | is subcommittee');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isSubcommittee([42]);
  assert.ok(result);
});
