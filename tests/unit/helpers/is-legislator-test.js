import { isLegislator } from 'enlightened-citizenry/helpers/is-legislator';
import { module, test } from 'qunit';

module('Unit | Helper | is legislator');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isLegislator([42]);
  assert.ok(result);
});
