import { concatCommitteeArray } from 'enlightened-citizenry/helpers/concat-committee-array';
import { module, test } from 'qunit';

module('Unit | Helper | concat committee array');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = concatCommitteeArray([42]);
  assert.ok(result);
});
